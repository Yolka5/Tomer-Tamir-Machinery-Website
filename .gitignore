# Dependencies
BeaverOS/node_modules/
node_modules/

# Large 3D assets (exceed GitHub 100MB limit)
*.gltf

# OS / editor
.DS_Store
Thumbs.db
.cursor/

# Original team photos (kept outside repo / web root)
../TTM-team-photos-private/
/yoni.JPG
/micah.png
/ItamarTold.jpeg
/tamir.jpeg

# Local API keys (copy from marcus-config.example.js)
BeaverOS/app/marcus-config.js

# Google service account key — NEVER commit this
*-sa-key.json
*service-account*.json
sa-key.json

# Generated wallet URL (contains a signed JWT — commit only if you want the
# badge live on the site; regenerate any time with generate-pass.js)
# wallet/wallet-url.js
